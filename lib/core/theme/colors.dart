import 'package:flutter/material.dart';

/// Constantes de colores para la aplicación Agenda Glam
/// Estas constantes se utilizan en toda la aplicación para mantener
/// una coherencia visual según la paleta definida en PLANNING.MD

// Paleta de colores base
const Color kPrimaryColor = Color(0xFF00296B);         // Royal Blue Traditional (AppBar)
const Color kPrimaryColorLight = Color(0xFF003F88);    // Marian Blue
const Color kPrimaryColorDark = Color(0xFF00509D);     // Polynesian Blue
const Color kAccentColor = Color(0xFFFDC500);          // Mikado Yellow (Acento/Botones)
const Color kAccentColorAlt = Color(0xFFFFD500);       // Gold (Acento alternativo)
const Color kBackgroundColor = Color(0xFF001B4D);      // Dark Royal Blue (Fondo)
const Color kSurfaceColor = Color(0xFF002C76);         // Dark Marian Blue (Superficies/Cards)
const Color kTextColor = Color(0xFFFFFFFF);            // Blanco (Texto principal)
const Color kSecondaryTextColor = Color(0xFFB0BAC5);   // Gris azulado (Texto secundario)
const Color kErrorColor = Color(0xFFCF6679);           // Rojo oscuro (Errores)

// Variantes de transparencia para efectos visuales
const Color kSemiTransparentPrimary = Color(0x8000296B);  // Royal Blue con transparencia
const Color kSemiTransparentAccent = Color(0x80FDC500);   // Mikado Yellow con transparencia

// Colores funcionales específicos
const Color kSuccessColor = Color(0xFF4CAF50);         // Verde para indicaciones de éxito
const Color kWarningColor = Color(0xFFFFC107);         // Amarillo para advertencias
const Color kInfoColor = Color(0xFF2196F3);            // Azul informativo
